/**
 * Caching layer.
 * Sprint 1 will implement local JSON file caching with TTL
 * and a force-refresh flag. Prevents redundant GitHub API calls
 * during development and keeps the dashboard responsive.
 */

export {};

/**
 * GitHub data fetchers.
 * Sprint 1 will implement functions to fetch merged PRs, reviews,
 * PR file paths, and issues for the configured time window.
 * Raw payloads are normalized into the shared types from @/lib/types.
 */

export {};
